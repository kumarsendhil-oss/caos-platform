# Stub / Incomplete Code Tracker

Per the CLAUDE.md convention: every stub, hardcoded placeholder, or
intentionally incomplete implementation in this codebase must carry a
`STUB(...)` marker in a comment, and that marker must resolve to something
tracked — either a real GitHub issue (`#123`) once this repo has a GitHub
remote and `gh` is authenticated, or an entry in this file
(`PENDING:NNN`) until then.

**All seven original stubs were filed as real GitHub issues on 2026-09-15**
against `kumarsendhil-oss/caos-platform` (issues #1–#7, label `stub`), and
every `STUB(PENDING:NNN)` marker in `services/api` was replaced with the
corresponding `STUB(#N)`. The ID column below keeps the historical
`PENDING:NNN` id so markers in older commits stay traceable.

For a **new** stub from here on, file the issue first and use `STUB(#N)`
directly:

```bash
gh issue create --title "<title>" --body "<body>" --label stub
```

Fall back to a `PENDING:NNN` marker (next unused number, plus a row here)
only if `gh` is unavailable or unauthenticated at the time.

Rows resolved without ever being promoted to a GitHub issue move to
**Resolved** below rather than staying in the open table with a resolved
status — `PENDING:007` set that pattern and `PENDING:008`–`013` follow it.
So the open table below is the whole of what is open, and its length is
the count.

## Open stubs

| ID | Location | Title | Blocked on | Status |
|---|---|---|---|---|
| PENDING:001 | `app/books_connector/tally_adapter.py` — `extract_purchase_register`, `extract_sales_register`, `extract_bank_book` | Implement Tally register extraction via XML-over-HTTP (TC-02) | Dev Readiness Checklist `P0-02` (port 9000 reachability), `ENV-05` (TallyPrime API Explorer sandbox access) | Filed — #1 |
| PENDING:002 | `app/books_connector/tally_adapter.py` — `post_entry` | Implement Tally voucher posting via XML import (TC-04) | `P0-02`, `ENV-05` | Filed — #2 |
| PENDING:003 | `app/books_connector/tally_adapter.py` — `connection_health` | Implement real Tally Cloud host ping for connection health (TC-05) | `P0-02` | Filed — #3 |
| PENDING:004 | `app/books_connector/zoho_adapter.py` — `extract_purchase_register`, `extract_sales_register`, `extract_bank_book` | Implement Zoho Books register extraction via REST API v3 (ZB-02) | `P0-06` (Zoho sandbox spike), `ENV-07` (Zoho developer account + sandbox org) | Filed — #4 |
| PENDING:005 | `app/books_connector/zoho_adapter.py` — `post_entry` | Implement Zoho Bill/Journal Entry posting (ZB-03) | `P0-06`, `ENV-07` | Filed — #5 |
| PENDING:006 | `app/books_connector/zoho_adapter.py` — `connection_health` | Implement Zoho OAuth refresh-token validation for connection health (ZB-05) | `P0-06` | Filed — #6 |
| PENDING:014 | `app/books_connector/tally_adapter.py` — import-response error handling (TC-04 / BK-07) | Tally's `Import Data` response does not reliably signal failure through its counters. Finding #17: a failed delete returned `LINEERROR: Cannot delete unnamed object: VOUCHER!` with **`ERRORS: 0`** and every other counter zero; a different failure of the same operation minutes earlier returned `ERRORS: 1`. A caller checking counters to decide success reads the first as a clean no-op success. `TallyAdapter` must treat the presence of `LINEERROR` as failure **regardless of the counters**, not check counters first and `LINEERROR` only when one is non-zero. Distinct from finding #2 (a success response where data silently did not land, caught by read-back) — #2 argues for reading back after a write, this argues for parsing the response itself more carefully; a guard against one does not catch the other, and both are needed | Not blocked — a design constraint for whenever `post_entry` / delete logic is actually built. Lands naturally on issue #2 (TC-04, Tally voucher posting via XML import) rather than needing its own issue now; stays a `STUB_ISSUES` line per `docs/CAOS-prompt-conventions.md` §4, since nobody is starting work on it and it blocks nothing in flight. No code carries a `STUB(...)` marker for it yet because the adapter method is still `NotImplementedError` under issue #2 | PENDING |
| PENDING:015 | `app/agents/` — BK-01 stock-item mapping (not yet written) | Resolving a stock item's effective GST rate and HSN code requires walking an inheritance chain, not reading the item master. Finding #20: `SRCOFGSTDETAILS` / `SRCOFHSNDETAILS` read `As per Company/Stock Group` on `Coastal Test Traders`' only item, and its five `RATEDETAILS` rows are all `GSTRATE 0` with no `HSNCODE` — those are deferral placeholders, not effective values. BK-01 must read the `SRCOF...` fields first and, when either defers, walk item → `PARENT` stock group → company. Compounding this, finding #18: rates are keyed by `GSTRATEDUTYHEAD`, scoped by `STATENAME` and dated by `APPLICABLEFROM`, so "the HSN code for this line" is a resolution over (item, parent chain, state, date), not a field read. Finding #7 already flagged invoice-line → stock-item *matching* as unscoped work comparable to BK-02's vendor-to-ledger matching; this is a **second** problem stacked on it, and neither is in BK-01 today | Not blocked — BK-01 does not exist yet (agents land Sprint 3+), and nothing in flight depends on it. Belongs to issue #28's remaining half, recorded there as a comment rather than as its own issue; stays a `STUB_ISSUES` line per `docs/CAOS-prompt-conventions.md` §4. No code carries a `STUB(...)` marker for it because no code exists to carry one. **Also unestablished:** what a stock-group or company-level GST master looks like on the wire, and whether the chain can terminate above those two levels — only the item level has been read | PENDING |
| PENDING:016 | `app/books_connector/tally_adapter.py` — master delete path, plus an upstream bug report | **Two things, one root cause (finding #22).** (1) **Adapter guard, mandatory:** `ACTION="Delete"` for a master whose target does not exist crashes TallyPrime with `Software Exception c0000005 (Memory Access Violation)` — process dead, full restart required. `TallyAdapter` must confirm the target exists via a **read** before every master delete; per findings #2/#3 a prior `CREATED: 1` is not sufficient evidence that it exists. On the practice's single-instance deployment (ADR 0001, TC-01) one such delete takes the integration down for every client on the box. Contrast finding #16: the same request shape against a nonexistent *voucher* returns `Voucher does not exist!` in 0.1s — object type is the only variable, and there is no way to tell which behaviour a master type has without triggering it. (2) **Also sharpens TC-05:** the port stayed `LISTENING` on the crashed process, so a TCP-connect health check reports healthy against a dead Tally. The health check needs a real request/response round trip with an explicit timeout — a third state, "dead but still listening", beyond #13's *unreachable* vs *unresponsive*. (3) **Upstream:** the payload was not malformed — it mirrors the documented `<LEDGER NAME="..." ACTION="Delete">` pattern with the object type changed — so this is plausibly reproducible on any TallyPrime instance and looks like an application bug worth reporting to Tally Solutions, independent of any adapter workaround | Not blocked — the adapter's delete path does not exist yet (issue #2, still `NotImplementedError`). Blocks nothing in flight, so it stays a `STUB_ISSUES` line per `docs/CAOS-prompt-conventions.md` §4. The upstream report is a human action with no repo artifact; the reproduction steps are in FINDINGS.md #22 if someone files it. No code carries a `STUB(...)` marker because no delete code exists to carry one | PENDING |
| PENDING:017 | `app/models/document.py` — `ExtractedData`, vs `docs/audit-platform-ER-core.mermaid` line 80 | ER diagram and model disagree: `EXTRACTED_DATA` carries `date invoice_date` in the diagram and the model has no such column | Not blocked — found during the issue #10 schema check (2026-09-16), not by a failure. CLAUDE.md requires the model and the ER diagram to match exactly, and the only sanctioned exception is `created_at`, which this is not — so one of the two is wrong and it is not yet established which. **Do not “fix” it by adding the column on sight**: `DraftEntry` already carries `invoice_date` as a required field and nothing persists it anywhere, so this is plausibly the same unpersisted-provenance problem as issue #10 rather than simple diagram drift, and it should be decided alongside #10 rather than ahead of it. Stays a `STUB_ISSUES` line per `docs/CAOS-prompt-conventions.md` §4 — nothing in flight depends on it. No `STUB(...)` marker: the defect is an absent column, which has nowhere to carry one | PENDING |
| STUB(#50) | `app/books_connector/base.py` — `DraftEntry.place_of_supply` / `supplier_state`, and every consumer of them | Three incompatible state representations in play, no mapping between them and no single source of truth | **Blocks the Sprint 1-2 `BooksConnector` / `DraftEntry` design** — surfaced by the issue #10 decision (2026-09-16), which needs a column type for these fields and could not pick one honestly. The three: (1) **two-letter alpha** — `DraftEntry` documents `place_of_supply` as `"TN"`, and P0-06 finding #8 shows live Bill responses echoing `"source_of_supply": "TN"` / `"destination_of_supply": "KA"`, so Zoho's own API is confirmed alpha on the wire — request and response (the original row inferred this from `docs/spikes/zoho_requests.py`, a pre-spike file predating `spikes/p0-06-zoho/`; right conclusion, assumed rather than observed); (2) **full state name** — Tally's `STATENAME` is a name, not a code (`Tamil Nadu`), and carries ``-prefixed sentinels such as `<EOT> Any`, seen live in `runs/2026-09-16T08-45-10-stockitem-master-dump`; GST rates are scoped by it (findings #18/#20), so `TallyAdapter` must produce it exactly; (3) **two-digit numeric** — the statutory GST state code (`33`), which is **already in the codebase implicitly** as the first two characters of `vendor_gstin`, a field both `DraftEntry` and `VOUCHER` carry. Nothing converts between any pair of these, and nothing declares which is canonical. **Two failure modes, one root cause — the original "silent" framing is Tally-specific (P0-06 finding #9).** On **Zoho** the same mismatch is *loud*: Zoho validates the tax choice against the two state codes rather than deriving it, rejecting symmetrically with **code 3032** (`TN`->`KA` with a CGST/SGST group: "IGST has to be applied as this is an interstate transaction"; `TN`->`TN` with `IGST18`: "IGST cannot be applied as this is an intrastate transaction") — a failed posting, not a wrong one. On **Tally** it is silent: intra- vs inter-state is decided by comparing `place_of_supply` against `supplier_state` (ADR 0011 Amendment 1), so a comparison across two representations does not raise — it returns unequal, picks IGST where CGST/SGST was correct, and posts a wrong-but-plausible split that per finding #2 the backend will not complain about. Decide the canonical form and put the conversions in the adapters (per ADR 0011, backend shapes belong at the edge, so alpha-to-`STATENAME` is `TallyAdapter`'s job, not the agent's) — and settle it **before** the Bookkeeping Agent starts populating the fields, not after. Related: `PENDING:017` and issue #10, all three being the same unsettled question about what `VOUCHER` records about a posted split; issue #10's column type for these two fields is provisional until this row is answered. **Promoted to issue #50 on 2026-09-16** per `docs/CAOS-prompt-conventions.md` §4: it now blocks work in flight, so it is no longer a lightweight row. P0-06 finding #9 is what changed — this stopped being a schema question (which column type) and became an architectural one (where the intra/inter *determination* lives, given both adapters need the same decision and duplicating it invites divergence). Both `post_entry` methods are still `NotImplementedError`; the issue surfaces the decision, it does not make it. No `STUB(...)` marker: the fields exist and are individually valid, so there is no incomplete implementation to mark — the defect is the absence of a decision between them | **RESOLVED 2026-09-17 — ADR 0011 Amendment 2.** Canonical form is the **two-digit statutory numeric** code (`33`), the only one of the three with a fixed government-assigned vocabulary; translation to alpha (Zoho) and `STATENAME` (Tally) happens at each adapter's edge; and the intra/inter determination moves **out** of the adapters into one shared function above them, superseding Amendment 1's per-adapter assignment. Issue #50 closed. Issue #10's provisional column type for these two fields is settled by this: numeric. **Implementation** of `determine_tax_jurisdiction()` and the CBIC table is Sprint 1-2 work landing with #2/#5 — the decision was the blocker, not the code |
| PENDING:019 | `spikes/p0-02-tally/reset_sandbox.py` — `verify_gone()` and `classify()`, plus `keep-coastal-services.txt` / `keep-coastal-test-traders.txt` | Delete verification is keyed on `VOUCHERNUMBER`, which finding #31 established is scoped **per voucher type, not per company** — `Coastal Services Ltd` now holds Purchase 1, 2, 5 and Sales 1, 2 simultaneously. Stated precisely, because the loose version is wrong: `classify()` matches the keep-list on a bare number, so the Sales vouchers match entries `1`/`2` and are **KEPT** — the protection layer over-protects and fails **safe**. The unsoundness is in `verify_gone()`, whose `still_there` / `expected` / `actual` sets are keyed on bare number and so **dedupe two vouchers numbered `1` into one set member** — it can mask a real collateral loss, or misreport a successful delete as a failure. That is the guard the script relies on to halt after a bad delete, so it is the layer that matters. Whether Tally itself disambiguates is a third, separate unknown: the delete payload does carry `VCHTYPE` alongside `TAGNAME`/`TAGVALUE` (#28), but that has never been tested. **Consequence, and it is not sandbox-specific:** any tooling or addressing assuming `VOUCHERNUMBER` is company-unique is unsafe on a company with more than one voucher type, which in production is every client company. **`reset_sandbox.py` must not be run against `Coastal Services Ltd` until this is resolved**; both Sales probe vouchers from Round 10 are deliberately left uncleaned rather than removed by a script whose verification cannot currently be trusted. No fix attempted in the Round 10 pass — resolution needs its own investigation and its own authorisation (establish whether `VCHTYPE` disambiguates; test `TAGNAME="MASTER ID"` per §6.3, still untested; then rekey on whatever survives — note `REMOTEID` is stable for *reads* (#15) but **not** confirmed addressable for *writes* (#16), so it is not automatically the answer). Guessing an untested addressing scheme is how #22 happened. **PARTIALLY RESOLVED 2026-09-16 — finding #33.** `verify_gone()` is **fixed**: every set now keys on `voucher_key()` = `(vchtype.casefold(), number)`, and an unexplained *appearance* halts alongside an unexplained vanishing. `classify()` gained a `Type:Number` keep-list spelling (`Sales:1`); bare numbers still protect every type, unchanged and still fail-safe. `keep-coastal-services.txt` was rebuilt type-qualified against a fresh live read — its old header claimed 4 vouchers, the company holds **6** (Purchase 1, 2, 4, 5 and Sales 1, 2). **The third layer is untouched and is why this is not closed:** whether `VCHTYPE` disambiguates a delete *Tally-side* is still untested, so a delete addressed at Purchase 1 may still take Sales 1 — the fixed guard would catch it, but only after the voucher is gone, and neither sandbox holds a safe-to-delete voucher. `reset_sandbox.py` therefore now **refuses to delete on any company holding more than one voucher type** unless `--allow-mixed-types` is passed (dry runs still enumerate and print the plan). **That refusal is tied to the addressing question, not to the guard fix** — retiring it on the wrong milestone is the obvious way to undo this | Not blocked — promoted to **issue #48** rather than left as a row, per `docs/CAOS-prompt-conventions.md` §4: this is a defect in code that **exists and is currently unsafe to run**, blocking a real workflow, unlike `PENDING:014`/`PENDING:016` which are design constraints for code not yet written. No `STUB(...)` marker: the defect is an unsound assumption in working code, not an incomplete implementation | Filed — issue #48 (guard fixed by #33; Tally-side addressing still open) |
| PENDING:020 | `docs/spikes/P0-04-whitebooks-gsp-runbook.md` and `docs/spikes/gsp_requests.py` — the whole GSP access path, and `ADR 0002`'s first stated consequence | GSTN per-taxpayer API consent (~30-day duration) plus a per-GSTIN OTP delivered to the **client**, not the practice — challenges ADR 0002's claim that the OTP/consent step "becomes a scriptable, automated step rather than a manual one" | **Blocked on verification, and a sandbox alone cannot supply it.** The practice can script the OTP request and submission but not its *receipt*, so if both constraints hold this is a recurring per-client human interaction on a ~30-day cycle — roughly 500 client contacts/month at the stated target, against a platform premise of 100→500 returns/month *without adding headcount*. **Nothing is verified**: no live request has ever been made against any GSP, session TTL is unknown, and whether a session refreshes without a fresh OTP is untested (if it does, much of this deflates). Two things are needed and only one is a spike — (a) a live sandbox run recording session TTL and refresh behaviour, which needs a WhiteBooks sandbox account that does not exist yet (`gsp_client_id` / `gsp_client_secret` are empty in `app/config.py`, and there is no `.env`); and (b) a **direct written answer from WhiteBooks**, because the 30-day consent duration is a *GST portal* property, not a GSP API one, and is not observable through the sandbox at all. A sandbox OTP flow may also be stubbed, which would leave the question open while looking answered. Blocks confidence in **RC-01** (Reconciliation Agent, Sprint 7) and **CC-05**'s scope, plus the Practice Management capacity model and the customer-facing feasibility narrative — not just P0-04. If confirmed, the outcome is an amendment in the shape of ADR 0011 Amendment 1 (the decision stands, one consequence was optimistic), **not** a reopening of ADR 0002. No `STUB(...)` marker: there is no incomplete implementation to mark — the gap is unverified documentation underpinning an Accepted ADR | **RESOLVED 2026-09-17 — issue #51 closed.** Confirmed from WhiteBooks' own ASP documentation, not inference: *"OTP will be sent to registered email and mobile number of your Client from GST Portal."* Not GSP-specific — WhiteBooks is a thin pass-through to `devapi.gst.gov.in`, so changing GSP would not change it, and ADR 0002's "scriptable, automated step" claim does not hold. Burden is narrower than feared: 6-hour `TXN` with OTP-free `refreshtoken`, so a client-delivered OTP is needed only when the portal-side access window lapses (~monthly), not per session. **Residual:** the portal-side duration is outside WhiteBooks' docs and stays well-attested but not vendor-confirmed, and no live call has been made — sandbox access needs a support call (their marketing claims gate-free self-serve signup; their ASP doc requires two support calls). Evidence: `docs/spikes/p0-04-whitebooks-gsp/whitebooks-api-reference.md` and the five source documents under `reference/` |
| PENDING:021 | `spikes/p0-07-dropbox/run_logger.py`, duplicating `spikes/p0-06-zoho/run_logger.py` | Run-logging helper copied between spikes rather than shared | Not blocked — a deliberate copy, tracked so it does not become silent duplication. The module is already generic (`RUNS_DIR` resolves from `__file__`; the header denylist is not vendor-specific), so P0-07's copy is byte-identical apart from its docstring and an added `log_webhook()` for inbound notifications — which P0-06 has no use for, since Dropbox is the first integration that calls **us**. Not imported across spike directories: that would couple two independent evidence trails. Not promoted to `spikes/_run_logger.py` in this pass either, because that means editing P0-06's committed scripts — a refactor, not scaffolding. **Extract when a third spike needs it**, following `PENDING:011` → `spikes/_args.py`, which is exactly this pattern one step later. Note `spikes/_args.py` and `spikes/_runner.py` already establish where a shared spike helper lives | Open — not filed |
| PENDING:022 | `.github/workflows/ci.yml` — workflow-level `on.pull_request` trigger | CI runs both jobs in full on docs-only PRs | Not blocked — costs runner time, blocks nothing | Open — the workflow has no `paths:` or `paths-ignore:` filter, so every PR to `main` runs **Backend (ruff + pytest)** and **Frontend (typecheck + build)** regardless of what changed. Observed on **PR #70**, a two-file docs change: it installed both the Python 3.11 and Node 20 dependency trees and ran the full backend suite plus a vite build against `services/`, which it does not touch — 34s + 18s, about a minute of runner time. Harmless at the current rate of docs PRs; worth doing if that rate rises. **Read this before applying the fix:** the obvious change is `paths-ignore: ['docs/**', '**.md']` on the `pull_request` trigger, and it is safe **only** because docs are covered by a separate workflow. `.github/workflows/md-tables.yml` (added 2026-09-19) runs `scripts/check_md_tables.py` and is filtered *to* `docs/**` — so ignoring docs in `ci.yml` does not leave docs PRs unchecked, but **collapsing the two workflows into one would**, and so would widening `ci.yml`'s ignore to cover paths `md-tables.yml` depends on. Do not reason about docs PRs as "nothing to check". **Not fixed when found, deliberately:** the change is to CI config, not docs, and this is pre-sprint — per `docs/CAOS-prompt-conventions.md` §4 it stays a lightweight row until someone starts on it or it blocks something in flight. No `STUB(...)` marker: `ci.yml` is not Python, the hook only guards `.py` files under `services/api`, and there is no incomplete implementation to mark — the workflow does exactly what it says, it is just scoped wider than it needs to be. **Separate change applied 2026-09-21 — this row stays Open.** The `push: branches: [main]` trigger was removed from **both** `ci.yml` and `md-tables.yml`, on the distinct finding that main is protected with `enforce_admins: true`, `allow_force_pushes: false` and `required_status_checks.strict: true` — so every commit reaches main through a PR, and `strict` makes the merge commit's tree byte-identical to the PR head's (`git rev-parse ae4b34d^{tree} ae4b34d^2^{tree}` returned the same hash), meaning the post-merge run re-tested a tree that had passed minutes earlier. Each workflow now carries a comment above its `on:` block naming `enforce_admins` and `strict` explicitly, so that loosening either one prompts restoring the trigger. **That change is not this row's fix and does not substitute for it:** this row is about `ci.yml` running the full backend and frontend jobs on docs-only **pull requests**, which still happens on every PR. The `paths-ignore` prescription above remains unapplied, and its caveat is unchanged and now load-bearing — both CI job names are required status checks, so a path filter that skips them leaves a docs-only PR waiting on contexts that never report, and applying it needs a skip-shim job publishing the same two context strings verbatim. One limit on the evidence behind the trigger removal, recorded so it is not rediscovered: only classic branch protection was read, not repository rulesets (`gh api repos/:owner/:repo/rulesets`), so the two required contexts are a floor rather than a verified complete set. A ruleset can only add requirements, never remove them, so this does not weaken the removal's safety argument — but anyone relying on the required-context list quoted here should query rulesets before trusting it as exhaustive |
| PENDING:023 | `docs/CAOS-coding-guidelines-v0.2.md` — CG2–CG11 scope, vs `scripts/` | Whether CG2–CG11 apply to repo-level Python outside `services/api/` is undecided | Not blocked — no second script exists yet | Open — CG1 was widened to cover `scripts/` in v0.2.1 (this repo's root `.ruff.toml`, added by PR #72), and says explicitly that it is the **only** rule reaching outside `services/api/`. That boundary is currently asserted rather than reasoned: CG2 (type hints) and CG3 (50-line functions) plainly *should* apply to a repo script and already do in practice — `scripts/check_md_tables.py` satisfies both — while CG5 (Decimal money), CG6 (external-call timeouts), CG7 (duplicate-prevention before books writes) and CG8 (Task Engine for human-judgment paths) are about platform behaviour a script does not have. CG10's ≥85% coverage gate is the genuinely open one: `scripts/` has no tests and no coverage gate today. Deciding this needs a second script to generalise from — one data point is not a pattern — so it waits rather than being guessed now. **Widening the document's "Backend (Python / FastAPI)" subtitle was considered and rejected** for this amendment: it invites exactly this question, which is a scoping exercise rather than a correction. No `STUB(...)` marker: nothing is incomplete in code, the gap is an undecided scope boundary in a document |
| PENDING:024 | `docs/audit-platform-ER-compliance-billing.mermaid` — `INVOICE`, vs CB-07 and PM-06 | `INVOICE` has no `due_date`, and `status` lacks `outstanding`/`overdue`, while two requirements need both | Not blocked — no model file exists for `INVOICE`; free to fix until Sprint 10 | Open — surfaced by the ADR 0015 gap report. `INVOICE.status` is `"draft \| approved \| sent \| paid"`. CB-07 specifies "Invoice history per client: sent, **outstanding**, paid status" and PM-06 specifies "**Outstanding/overdue** invoice visibility, sourced from the Client Profiling & Billing module". Neither state exists in the enum, and *overdue* is not derivable either, because there is no invoice `due_date` field to compare against — only `billing_period`. So two requirements name three states the schema cannot represent or compute. **Not fixed in ADR 0015 deliberately:** 0015 is scoped to the task and service models, and inventing an invoice payment-state lifecycle is a billing decision that belongs with whoever scopes Sprint 10, not a side-effect of a task-model ADR. Whether `outstanding` is a stored status or derived from `sent` + `due_date` + payment records is itself the open question. No `STUB(...)` marker: `INVOICE` has no model file — the entity exists only in the diagram, and the hook only guards `.py` files under `services/api` |
| PENDING:025 | `docs/audit-platform-ADR-0015-engagement-and-task-model.md` — `ENGAGEMENT.billing_frequency`; `docs/audit-platform-ER-core.mermaid` — `CLIENT.billing_cycle`; PRD CB-01 | No `annual` or `on_completion` billing value exists anywhere in the platform | Not blocked — needs the practice to confirm which cycles they actually bill on | Open — surfaced by the ADR 0015 gap report. Every representation of a billing cycle is two-valued: CB-01 says "billing cycle (monthly/quarterly)", `CLIENT.billing_cycle` is `"monthly \| quarterly"`, and `services/api/app/models/client.py` carries `billing_cycle` defaulting to `"monthly"`. ADR 0015's `ENGAGEMENT.billing_frequency` inherits the same two values rather than widening them. Statutory audit and ROC work is plausibly annual, and advisory work plausibly bills on completion — *inference from the CB-02 service categories, not from anything the practice has stated*, which is exactly why this is a question and not a fix. **Deliberately not widened in ADR 0015:** adding enum values the customer has not asked for would be guessing at their billing model, and `fee_basis` (`fixed \| hourly \| per_return`) is a separate axis that may already cover the on-completion case. Needs the proprietor's input, in the same way ADR 0005 says the routing rule table does. No `STUB(...)` marker: no model file for `ENGAGEMENT`, and `Client.billing_cycle` is a complete implementation of a too-narrow decision rather than an incomplete one |
| PENDING:026 | `docs/audit-platform-ER-compliance-billing.mermaid` — `BILLABLE_EVENT` | `BILLABLE_EVENT` has no `status`, so there is no work-in-progress view | Not blocked — no model file exists for `BILLABLE_EVENT`; free to fix until Sprint 10 | Open — surfaced by the ADR 0015 gap report. `BILLABLE_EVENT` is `id, invoice_id, task_id, description, amount`, with `invoice_id` as a plain FK. It therefore only exists once it has been line-itemised onto an invoice, which means the platform has no representation of billable work that has been done but not yet invoiced — no WIP. A `status` of `accrued \| invoiced \| written_off` (with `invoice_id` nullable until invoicing) would give CB-04's billing-trigger mapping somewhere to write on task completion, rather than work being invisible until an invoice is generated. This matters more after ADR 0015 than before it: `TIME_ENTRY` plus `TASK.work_class` make accrued-but-unbilled effort computable for the first time, and written-off work is the other half of the utilization picture ADR 0013 wants. **Not decided in ADR 0015:** the WIP lifecycle is a billing decision, and the ADR deliberately left `SERVICE_OVERRIDE` and the invoice path untouched. No `STUB(...)` marker: `BILLABLE_EVENT` has no model file |
| PENDING:027 | `docs/CAOS-PRD-v0.2.md` §5.14 (PM-05) and `docs/CAOS-sprint-plan-v0.2.md` Sprint 11 | PM-05's own description is known-false, and its Sprint 11 placement is questionable now that time data is the primary value driver | Not blocked — a scoping decision, needs the proprietor and whoever plans the sprints | Open — two things, one root cause. (1) **The description is false and both documents say so.** PM-05 reads "Time-per-task tracking — extends the practice's existing task-sheet habit rather than replacing it outright", and the sprint plan's own v0.2.2 changelog records the P0-01 finding that "the practice is not tracking timesheets at all, so the item's premise is false rather than the data merely being unavailable." The sprint plan flags this and explicitly leaves it unrescoped: "PM-05 is therefore likely *introducing* time tracking rather than extending it, which is a different design and adoption problem." `docs/context.md` carries the same finding, and notes the customer-facing time-savings figures were corrected to discovery estimates for the same reason. (2) **The sequencing is now questionable.** PM-05 sits in Sprint 11 (Weeks 21–22) because it was a dashboard nicety over data assumed to exist. The ADR 0015 customer review makes time measurement the platform's centre of gravity, and `TIME_ENTRY` is what P0-01's deferred baseline is waiting on — so the first measured baseline cannot begin accruing until Week 21 at the earliest. **ADR 0015 deliberately does not resequence:** the ADR was scoped to the data model, and moving sprints is a separate decision with its own dependencies (Sprint 10's catalog work also grew under 0015 Decision 2). Flagged there under "Not decided here", tracked here. No `STUB(...)` marker: both are documents, not code |
| PENDING:028 | `services/api/app/models/` — no attendance model; `docs/audit-platform-ER-core.mermaid` and `docs/audit-platform-ER-compliance-billing.mermaid` — no attendance entity | The attendance schema is unwritten, in both the model layer and both diagrams | Not blocked by the vendor question — required under any `P0-08` outcome | Open — surfaced by ADR 0016 Decision 4. ADR 0013 said attendance is "built natively in CAOS" and named **no entity, no fields and no storage**; ADR 0016 carries that decision forward unchanged and still does not write the schema. Neither `.mermaid` declares an attendance entity and `app/models/` has no attendance model, so the feature has never had a schema anywhere. 0016 states the **obligations** rather than the shape — user, event type, timestamp, geo coordinates, accuracy, device, source — and deliberately stops there. **The schema is required regardless of how `P0-08` resolves**: CAOS is the sole check-in surface, performs geofencing validation itself, and owns the utilisation denominator (0016 Decision 3), so what the vendor accepts changes what CAOS *exports*, not what it must *record*. One field is genuinely spike-dependent (what `source` may contain, per `P0-08` T1), which is why 0016 declined to guess. Also undecided: whether the entity belongs in the core or compliance-billing diagram. No `STUB(...)` marker: the defect is an absent model file, which has nowhere to carry one |
| PENDING:029 | `docs/CAOS-api-spec-v0.2.md` §15 — `GET /capacity`; PRD §5.14 PM-03 | PM-03's response shape changes from headcount to hours, breaking its specified contract | Not blocked — the endpoint is unbuilt; the replacement shape needs specifying | Open — surfaced by ADR 0016 Decision 6. §15 specifies `GET /capacity` returning `{available_staff: int, at_risk_clients: []}`, tracing to PM-03 ("Capacity indicator — surfaces available bandwidth for new client work"). **A headcount cannot answer that requirement**: four available staff reads identically whether those four are near capacity or barely used, so the response returns *headcount* where the requirement says *bandwidth*. 0016 decides PM-03 becomes **hours-based** — available hours against committed hours, from attended minutes (0016) net of In Progress minutes and `standard_minutes` (ADR 0015 Decision 4) — which changes the response shape. **0016 deliberately does not specify the replacement**, and the API spec is not edited in that pass. Adding hours *alongside* `available_staff` was considered and rejected there: consumers would keep reaching for the existing field, and two disagreeing capacity numbers is worse than one right one. Note this is the **user-visible payoff of 0015 and 0016 together** — neither delivers it alone. No `STUB(...)` marker: both are documents, not code, and the endpoint does not exist to carry one |
| PENDING:030 | `app/agents/` — TDS/TCS Return Automation Agent (not yet written); ADR 0013's Integration Architecture, Form 24Q clause | The TDS agent's scope is conditional — the 26Q/27EQ narrowing may reverse | **Gated on `P0-08` T4** (does RazorpayX file Form 24Q) — and the spike has not been run | Open — surfaced by ADR 0016. ADR 0013 narrowed CAOS's TDS agent to **26Q/27EQ (non-salary TDS)**, with 24Q "owned end-to-end by Frappe HR / India Payroll" and CAOS reflecting its filing status "for dashboard visibility only — avoiding duplicate logic between the two systems." That narrowing was a **consequence of the vendor choice, not an independent decision about the agent**, and its premise is now unverified: the customer dropped Frappe, and nobody has checked whether RazorpayX files 24Q. **If T4 establishes that it does not, the narrowing reverses and the agent's scope grows to include Form 24Q** — a materially larger agent than the one specified today, landing on whichever sprint owns the TDS module. 0016 flags this as **conditional, not decided**, because it turns entirely on a vendor fact and is not decidable from any document in this repo. **Nothing should be built against either scope until T4 reports.** No `STUB(...)` marker: the agent does not exist yet (agents land Sprint 3+), so there is no code to carry one |
| PENDING:031 | ADR 0016, "New surface: employee PII held by an external processor"; `docs/CAOS-PRD-v0.2.md` §8 item 4 | Employee PII, including continuous location traces, moves to an external SaaS processor — unassessed | **Needs the practice's legal contact**, not a technical answer | Open — surfaced by ADR 0016, and it is the one genuinely new risk the vendor change introduces. A self-hosted Frappe HR instance kept employee identity, salary, bank details and statutory identifiers inside infrastructure the practice controls; **RazorpayX Payroll is SaaS**, so an external party processes and holds them — plus, under 0016 Decision 4, **geofenced location traces about identifiable employees**. **This cuts against ADR 0004 twice.** 0004's consequence is that the platform "narrows the DPDP Act surface considerably" and it rejected a data warehouse for "expand[ing] the DPDP surface significantly" — this *widens* it. More sharply, 0004's narrowing argument rests on the data already sitting in systems the practice already uses (Tally, Zoho, Dropbox); **employee data has no such pre-existing custodian**, so there is no "the practice already accepted this posture" to fall back on. **Deliberately not resolved in 0016**, which claims neither that the arrangement is compliant nor that it is not — only that the surface changed and nobody has assessed it. Routed the way this project already routes DPDP questions (PRD §8 item 4, §10 item 1) and the way ADR 0013 routed its own GPL-3.0 question: "needs confirmation from counsel before production use, not assumption." Related: `P0-08` T9 (what PII the vendor holds) establishes the facts counsel would need, but does not answer the question. No `STUB(...)` marker: the gap is an unresolved legal question over a document, not an incomplete implementation |
| PENDING:032 | `docs/CAOS_Feature_Documentation_v0_6.docx` — “Task Management & Practice Measurement”, the waiting-states subsection; vs `docs/audit-platform-ADR-0015-engagement-and-task-model.md` | The customer-facing documentation describes task blocked states that no ADR proposes | **Now blocked on the practice, not on us.** The amendment is written; what it cannot settle is the vocabulary question it puts to the proprietor (are the two reasons the whole list, and does internal waiting need one?). Acceptance of the amendment is the other half | Open — surfaced while writing Feature Documentation v0.6 (2026-09-20). The v0.6 section describes two waiting states, **“waiting on client” and “waiting on system”**, and explains what the waiting-on-client figure reveals about turnaround. **No ADR proposes either.** ADR 0015's corrected Decision 4 measures “Blocked duration, by `blocked_reason`”, but its own **Follow-up section explicitly defers adding a blocked state and `blocked_reason` to any Decision** — “Not proposed here” — and no other Decision covers them. `STATUSES` in `app/models/task.py` is `open \| in_progress \| completed \| escalated`, with no blocked member and no reason column anywhere. The vocabulary came from the **2026-09 customer review**, which is where the v0.6 wording came from too. **This is not a false claim:** the v0.6 section is marked not-yet-built throughout, gives delivery references only where they exist, and marks the rest not yet scheduled. The problem is ordering — **the customer document is ahead of the decision record**, which inverts the normal direction on this project, where an ADR settles a thing and the customer-facing docs follow. Related: the same section's manual-then-automatic waiting reason is also unbacked by any ADR, and `PENDING:027` already records that the sequencing of this whole area is undecided. **Needs an ADR (or an ADR 0015 amendment) covering the blocked state and `blocked_reason` before the customer treats v0.6 as a commitment** — whichever lands, it should also settle whether the two reasons are the whole vocabulary or the first two of a longer list. **Updated 2026-09-21 — still Open, and now higher priority.** Two things changed, neither of which closes this. **ADR 0015 is now Accepted** (2026-09-21) — but acceptance does not resolve this row, because 0015 still defers the `blocked` state and `blocked_reason` to its Follow-up section rather than deciding them. An accepted ADR that defers a decision has deferred it just as firmly as a proposed one did; the gap is unchanged in substance and only its provenance moved. **And `CAOS_Feature_Documentation_v0_6.docx` has now been shared with the practice.** That is what raises the priority: the customer is reading a description of task states that no accepted ADR covers, so the documentation is ahead of the decision in front of the person who will hold us to it. Until now this was an internal inconsistency; it is now an external commitment without a backing decision. **Queued as the first ADR work of Sprint 1** — an ADR 0015 amendment settling the blocked-state vocabulary, per conventions §2 (0015 is Accepted, so this is an amendment, not an edit in place). It still needs to settle whether the two reasons are the whole vocabulary or the first two of a longer list. No `STUB(...)` marker: no model file exists for this, the defect is an absent decision rather than an incomplete implementation, and the hook only guards `.py` files under `services/api`. **Updated 2026-09-21 — amendment proposed, still Open.** `docs/audit-platform-ADR-0015-amendment-1-blocked-state.md` proposes the blocked state, `blocked_reason`, and the two-value vocabulary, resolving 0015's Follow-up deferral. **It is Proposed, not Accepted, so this row does not close** — and acceptance alone will not close it either: the amendment puts the extensibility question to the proprietor, and until that is answered the customer document still describes a vocabulary the decision record has not fully fixed. **Closes when the amendment is Accepted *and* the vocabulary question is answered.** The amendment's blocked-versus-escalated Open question is tracked separately as `PENDING:035` and does not gate this row |
| PENDING:033 | `docs/context.md` — currency of the whole file; no checker exists | **Nothing mechanically verifies that `context.md` is current against merged work.** Its own footer states the test — if the file is stale, that means `/wrapup` is not being run — but nothing enforces it, so the failure is silent and is only found by a human noticing a contradiction. It did fail: on 2026-09-21 the file was last updated 2026-09-18 with PRs #77–#80 merged since, and it took a documentation audit to spot. A check could compare the newest `Merge pull request #NN` on `main` against the highest PR number cited in `context.md`, or simply compare the file's last-commit date against `main`'s, and fail when the gap exceeds some threshold. Both are approximations — neither can tell whether the *content* is accurate, only whether the file has plausibly kept up | Not blocked — needs a decision on what the check actually asserts before anything is written. The 2026-09-21 `/wrapup` change makes the context.md step unconditional and merges it in the same PR as the work it describes, which removes the mechanism that produced the observed drift; this row is about verifying that rather than trusting it | Open — deliberately deferred, not forgotten. The argument for it is `docs/CAOS-prompt-conventions.md` §9 applied to itself: a manifest verified by a check is redundant with the check, and a convention nobody runs is worth less than a check that runs itself. The same reasoning produced `scripts/check_doc_versions.py`, which is the precedent — it replaced a version manifest that went stale the day it was rebuilt. Deliberately **not** wired into `.claude/hooks/post_edit_docs.py` if it is ever built: that hook fires per-edit, and context.md currency is a property of the repo between sessions, not of any single edit — the same reason `check_doc_versions.py` is excluded from it. CI or `/verify-done` is the right home. No `STUB(...)` marker: `docs/context.md` is not Python, the hook only guards `.py` files under `services/api`, and there is no incomplete implementation to mark — the gap is an absent check, not unfinished code |
| PENDING:034 | `.github/workflows/md-tables.yml` — the `paths:` filter for `check_readme_refs.py` | **The filter listing the non-`docs/` paths `docs/README.md` references is hand-maintained and will not track new references.** `scripts/check_readme_refs.py` resolves every reference in `docs/README.md` against the git index, and some resolve outside `docs/` — today `CLAUDE.md`, `README.md`, `spikes/**` and `services/api/app/models/**`, which the filter names explicitly so a PR that deletes or renames one of them actually runs the check. Add a row to `docs/README.md` pointing at a new top-level path and the filter does not know about it: a later PR could delete that path and the check would not run, so the stale row reaches `main` — exactly the drift the checker exists to stop, re-introduced one level up. The filter is a false negative only, never a false positive: it can skip a run, never fail a good PR | Not blocked — needs a decision on which fix. Three options, none obviously right: (a) drop the `paths:` filter entirely so the job always runs, which is honest and costs a fast stdlib job on every PR but gives up the filter independence the workflow's own header comment says it was split from `ci.yml` to keep; (b) have `check_readme_refs.py` emit the path list and a second check assert the filter matches it, which is a check on a check; (c) leave it hand-maintained and accept the gap, which is the status quo | Open — deliberately deferred, not forgotten. Same shape as `PENDING:033` and filed for the same reason: the gap is an absent check, not unfinished code. **No `STUB(...)` marker**, on `PENDING:033`'s precedent — `.yml` is not Python, `.claude/hooks/post_edit_python.py` only guards `.py` files under `services/api`, and there is nothing incomplete to mark. Worth noting the option-(a) argument is stronger than it first looks: the job installs nothing but a pinned ruff and the whole suite runs in seconds, so "always run it" is cheap, and CG1's pinned-dependency clause stays unengaged either way |
| PENDING:035 | `services/api/app/task_engine/service.py:131` (`escalate_overdue`) vs `docs/audit-platform-ADR-0015-amendment-1-blocked-state.md`; touches ADR 0005 / TE-04 | **How a blocked task interacts with escalation is undecided, and it gates implementing the blocked status.** `escalate_overdue()` sets `status = "escalated"` unconditionally on any overdue task. ADR 0015 Decision 4 decided `due_at` does **not** pause while a task is blocked, so an overdue blocked task would be moved out of blocked. Under Amendment 1's invariant that `blocked_reason` is non-null iff `status = 'blocked'`, leaving blocked clears the reason; under Decision 4's definition of the blocked clock — into-blocked transition to out-transition — the move to `escalated` **ends the blocked interval**. The recorded wait would then be shorter than the real one, and a task escalated *because* it is still waiting on a client would read as no longer waiting — truncating precisely the cycle-time figure the measurement exists to produce | **A decision reaching into ADR 0005 / TE-04**, which is why Amendment 1 does not make it. Three options, none chosen: **(a)** `blocked -> escalated` ends the blocked interval — consistent with the invariant and with Decision 4 as written, no TE-04 change, but understates the wait; **(b)** escalation becomes a flag orthogonal to `status` — cleanest, since accountability and workflow state stop competing for one column and a task can be both blocked and escalated, but it changes what `escalated` means in ADR 0005/TE-04 and touches every consumer reading `status = 'escalated'`; **(c)** `escalate_overdue()` raises the escalation without a status change for blocked tasks — narrower than (b), still a TE-04 change, and leaves TE-04 with two behaviours depending on status | Open — **an implementation gate, not a live defect.** No blocked status exists in code today, so `escalate_overdue()` is correct for the statuses that do exist and needs no change now; it becomes wrong the moment `blocked` is added. **Must be settled before the blocked status is implemented.** Raised by ADR 0015 Amendment 1's Open question 1; related to `PENDING:032`, which tracks the customer-documentation gap the amendment addresses and which this row does not gate. No `STUB(...)` marker: `service.py` is correct as written for today's status vocabulary, so there is nothing incomplete to mark — the gap is an unmade decision about a future status |
| PENDING:036 | `docs/CAOS-sprint-plan-v0.2.md` Sprint 1–2 exit criteria and Sprint 4–5; `app/books_connector/tally_adapter.py`, `zoho_adapter.py` | **Both adapters need verification against real systems before the Sprint 4–5 end-to-end test, and Foundation no longer covers it.** The 2026-09-21 reconfirmation made Foundation **sandbox-first**: it exits on the TallyPrime API Explorer / a test company and a Zoho developer or test org. Real-system verification moved out of Foundation and became a named gate before Sprint 4–5's critical end-to-end flow. Without a row, that gate exists only as a sentence in a sprint plan and is exactly the kind of thing that gets skipped when Sprint 4–5 starts under pressure — the same failure that left the timeline reconfirmation itself untracked for five days | **The practice, not engineering.** Needs a **Tally Cloud test company** and a **Zoho organization**, with real `ENV-05` and `ENV-07` credentials — neither exists as a repo secret today (`.github/workflows/ci.yml:86` records why the integration suite is unwired). `ENV-06` (WhiteBooks GSP) is the same shape but gates GSP health rather than the adapters, and `P0-04` still needs a sandbox account obtained via a support call | Open — **a confidence gate, not an implementation gate.** Switching an adapter from a sandbox to a client account is **configuration, not code**: a per-client `BooksConnection` row and its OAuth refresh token, so nothing about either adapter changes at that boundary. **Never to be run against live client books** — a test company or a separate organization only. Note the asymmetry P0-06 established: Zoho rejects a wrong-direction tax loudly (code 3032, finding #9) while Tally accepts a wrong split silently, so the Tally side of this gate needs a designed verification rather than "it posted without error". No `STUB(...)` marker: both adapters already carry `STUB(#1)`/`STUB(#2)`/`STUB(#4)`/`STUB(#5)` for the unimplemented methods, and this row is about verifying them once written, not about the stubs |
| PENDING:037 | The Zoho organization used for build-time development; `spikes/p0-06-zoho/FINDINGS.md` Open questions | **The Zoho org Foundation is built against may expire mid-build, and the one P0-06 used may already be unusable.** P0-06's own Open questions carry "Premium Trial expiry date — check Settings → Subscription before spike work spans past it", and that spike ran on 2026-09-16 against an "Integra Agro" trial org. Foundation now runs six weeks of Zoho adapter work (ZB-01/02/04); a trial lapsing partway through costs the org's configuration, its tax rates and its OAuth grants, and the adapter work stalls on something with a commercial lead time rather than an engineering fix | Not blocked technically — needs someone to **check the current org's expiry and, if it is a lapsing trial, provision a durable developer org before Week 1**. Deliberately **not** folded into `PENDING:036`: that row is a pre-Sprint-4–5 dependency on the practice, this one is a **pre-Week-1 action that is ours**, and burying a Week 1 task inside a Week 9 gate is how it gets missed | Open — **check before Foundation starts, not during.** Cheap now, disruptive at Week 3. Two things to confirm together: whether the P0-06 org is still live, and whether it is the right org to build against at all. Related but distinct: `PENDING:036` (real-system verification, later, practice-supplied) and P0-06's other open items — refresh-token lifetime and revocation behaviour on this DC (`OQ-06`), which the same check could answer cheaply. No `STUB(...)` marker: the gap is an unverified external account, not incomplete code |

**Notes, not yet tracked rows (2026-09-16)**

- **`PENDING:016` is unaffected by the #16 correction.** It concerns
  finding #22 — deleting a **master** that does not exist crashes
  TallyPrime — and #28 concerns **vouchers**. #22 itself established
  that the two object types behave differently on delete, so nothing
  learned about vouchers today transfers to it. The row stands exactly
  as written.
- **Candidate for a future row: does master deletion want
  `TAGNAME`/`TAGVALUE` too?** #22's crashing attempt used
  `<STOCKITEM NAME="..." ACTION="Delete"/>`, and #28 showed that for
  vouchers the `NAME=`/child-element schemes are refused while
  `TAGNAME`/`TAGVALUE` works. It is *possible* #22's crash was wrong
  addressing plus an absent target rather than a fundamental limit.
  **Hypothesis only — not tested, not authorised, not scheduled**, and
  today's voucher results do not lower #22's risk. Testing it needs a
  fresh session with its own authorisation, and is blocked anyway on
  creating a stock item at all (the stock-group problem in #22). Left
  as a note rather than promoted to a row per
  `docs/CAOS-prompt-conventions.md` §4 — nobody is starting work on it
  and it blocks nothing in flight.


## Resolved

| ID | Location | Title | Issue | Outcome |
|---|---|---|---|---|
| PENDING:007 | `app/task_engine/service.py` — `escalate_overdue` | Route escalation to a specific role/person via TE-02's routing rule table, instead of only flipping task status | #7 | Resolved — TE-02 routing rules implemented; `RoutingRule` model, migration, and `GET`/`PUT /admin/routing-rules` landed. Escalation now targets the role above the routine owner. |
| PENDING:008 | `spikes/p0-02-tally/no_inventory_test.py` — `__main__` argv parsing | `--company` took its value with no validation: `IndexError` when the flag was last with no value, and the next flag silently accepted as the company name. `--company --send` targeted a company called `--send` **and still posted live** — consuming `--send` as a value did not remove it from `sys.argv`, so the `"--send" not in sys.argv` dry-run guard did not catch it | — (never promoted) | Nothing — resolved by `fix/no-inventory-test-company-validation`, a copy-across of `post_voucher.py`'s `company_from_argv()` (PR #29). Never promoted to a GitHub issue, so no closing keyword |
| PENDING:009 | `spikes/p0-02-tally/` — both sandbox companies | Reset the sandbox companies to a known voucher state | — (never promoted) | **Resolved 2026-09-16.** `spikes/p0-02-tally/reset_sandbox.py` exists and its destructive path is validated end to end against a live delete (finding #29). Because a keep-list audit that same session found **every voucher in both companies cited by a finding** — Coastal Services 1, 2, 4, 5 and Coastal Test Traders 1-8 — validation used a purpose-built target: one voucher posted marked `PENDING009-VALIDATION-DELETE-ME` on #21a's accounting-view shape with distinct amounts (1000/90/90/1180), so the Trial Balance delta could not be confused with vouchers 2 or 5. Sequence: post -> read-back (stored as voucher 6) -> dry run showing exactly 1 DELETE and 4 KEEP -> `--confirm` -> independent re-read. `DELETED: 1`, no `LINEERROR`, exit 0, and the Trial Balance returned exactly to its pre-post figures (Purchase `-55,200`, Current Liabilities Dr `-9,938` / Cr `65,138`), which proves the delete removed exactly the voucher added and nothing else. All four survivors held `ALTERID`, `REMOTEID` and amounts although the company-wide sequence advanced to 10 — extending #28's no-renumbering result to a full post-and-delete cycle. Safety as built: dry run by default, `--company` required with no default, runtime keep-list with nothing hardcoded, Day Book enumeration (#11, never `TYPE:COLLECTION`), one delete at a time with read-back after each, halt with no retry on `LINEERROR` (#17) / target still present / collateral loss / count off / no response, and a `--max` ceiling. Cancelled vouchers and vouchers with no `VOUCHERNUMBER` are SKIPped and reported rather than addressed by a guessed scheme. Guards covered by `reset_sandbox_offline_checks.py`, which needs no live Tally. **Remaining limits, recorded not open:** masters still cannot be reset through the API (#22, `c0000005`), and the manual procedure stays the only option there; both keep-lists currently protect every voucher their company holds, so the script has no routine work until new debris accumulates — rebuild a keep-list by re-running the audit, never from memory. Never promoted to a GitHub issue, so no closing keyword |
| PENDING:010 | `app/books_connector/tally_adapter.py` — post-verification correlation (BK-07) | Carry-forward of finding #10's open item and #11's `REMOTEID`/`VCHKEY` note. #14 rules out `VOUCHERNUMBER` (Tally auto-assigns it). **#15 narrows this to `REMOTEID`, not `VCHKEY`**: `REMOTEID` is byte-identical to the voucher's own `GUID`, stable across repeated reads, and — as of the 2026-09-16 restart test — stable across a full TallyPrime restart. `VCHKEY` stays demoted on structural grounds (opaque constant plus storage offset); note the restart test disproved #15's guess that its `0000b49a` segment was a build/session handle, so that segment is durable but still unexplained. `TallyAdapter` should capture `REMOTEID` at post time | — (never promoted) | Nothing — **resolved 2026-09-16**. `REMOTEID` is the confirmed correlation field for `TallyAdapter.post_entry`, stable across all three conditions: repeated reads, a full TallyPrime restart (`runs/2026-09-16T06-49-*` vs `06-55-*`), and an edit to an existing voucher (`runs/2026-09-16T07-18-22-*` pre-edit vs `runs/2026-09-16T07-23-13-*` post-edit, amount changed and tax split recomputed). On the edited voucher only `ALTERID` moved (1 → 5, a company-wide alteration sequence — not a per-voucher revision count); `REMOTEID`, `VCHKEY`, `GUID`, `VOUCHERNUMBER` and `MASTERID` all held, and the three untouched vouchers were unaffected. This covers the BK-07 amended-voucher case, which is now tested rather than assumed. No remaining risks. Never promoted to a GitHub issue, so no closing keyword |
| PENDING:011 | `spikes/p0-02-tally/` — `company_from_argv()` duplicated three times | `post_voucher.py`, `no_inventory_test.py` and `remoteid_stability_probe.py` each carried a byte-identical copy. Deferred until a third copy appeared or one of the scripts was next edited — both became true | — (never promoted) | Nothing — was deliberately deferred, not blocked **Resolved** — extracted to `spikes/_args.py`, beside `_runner.py`, exporting `flag_value(argv, flag, what)` as the primitive and `company_from_argv(argv, default)` as a named wrapper on it. All three scripts import it; no local copies remain. The bodies were byte-identical as this row noted, but the surrounding context was not: each closed over a different module-level `COMPANY` (`Coastal Test Traders` vs `Coastal Services Ltd`), so `default` is a **required positional** — a script silently inheriting another's target company would post to the wrong books. Verified as a pure refactor: pre-extraction output captured to file and diffed against post-extraction, not compared by eye — missing-value and flag-as-value on all three scripts, `--baseline` with no value, and both default dry-run payloads, all byte-identical. |
| PENDING:012 | `spikes/p0-02-tally/remoteid_stability_probe.py` — `main()` closing banner | Banner unconditionally prints "not the same as stable across a restart" regardless of whether a restart comparison was actually run, and the script has no cross-run diff capability — it only diffs its own two reads within one invocation. Misleading now that the restart workflow is exactly two separate invocations compared manually (2026-09-16, finding #15), where the banner contradicts the result that was just established | — (never promoted) | Nothing — resolved by `fix/probe-cross-run-baseline` (`dbc035f`), which replaced the static banner with three cases (no baseline / baseline matched / baseline moved) and added `--baseline PATH` for real cross-run comparison against a prior run's artifacts. Deliberately not auto-detecting recent runs: the script can see that two artifact sets differ in time, not what changed between them, so the human asserts the condition and the script checks only whether the identifiers moved. Never promoted to a GitHub issue, so no closing keyword |
| PENDING:013 | `spikes/p0-02-tally/remoteid_stability_probe.py` — `FIELDS` and the `--baseline` banner | Two gaps with one root cause: the probe reads identifier fields only, so it has no context for reasoning about what a movement means. (1) **All field movement is treated as failure.** `FIELDS` mixes identity candidates (`REMOTEID`, `VCHKEY`, `GUID` — movement is bad) with change-tracking fields (`ALTERID` — movement is the expected, correct result of a real edit), and the banner reports any movement as "these identifiers did not survive it". On the 2026-09-16 run that *resolved* `PENDING:010`, it read a successful result as a failure. (2) **A void run is indistinguishable from a real one.** Reading no amounts, the script cannot tell "nothing changed" from "the asserted condition never happened", nor which voucher was edited — both had to be hand-checked against the response XML on that same run, once catching an unsaved edit that otherwise read clean | — (never promoted) | Nothing — **resolved 2026-09-16** by `fix/probe-field-categories`. (1) `FIELDS` is now three named categories rather than one flat tuple: `IDENTITY` (`REMOTEID`, `VCHKEY`, `GUID`) — movement defeats correlation and is reported as FAILURE; `CHANGE_TRACKING` (`ALTERID` alone) — movement alongside stable identity is reported as EXPECTED, since that is what a genuine edit is supposed to look like; and `CONTEXT` (`VOUCHERNUMBER`, `MASTERID`) — reported as NOTEWORTHY, not a verdict. **`MASTERID` is deliberately CONTEXT, not either of the other two**: it held at 1 through the 2026-09-16 edit so it is not change-tracking, but finding #14 rules out Tally-assigned sequence numbers as correlation keys and neither context field has been tested under a rewrite or reimport, so calling it identity would silently re-promote a field #14 demoted. Field values are now stripped before comparison — Tally pads element text (`' 5'`) and whitespace is not movement. The within-run banner deliberately does *not* use the categories: nothing altered a voucher between two back-to-back reads, so **any** movement there, `ALTERID` included, is still a failure. (2) Each voucher's `AMOUNT` list is now carried alongside its identifier fields and the cross-run banner names which vouchers moved and on what, so a void run is distinguishable from a real one and the edited voucher is identified without hand-checking the response XML. Amounts are evidence the asserted condition landed, not a fourth category — they carry no verdict of their own. Re-verified offline against the existing artifacts, no live Tally and nothing posted: `07-18-22` vs `07-23-13` (the run that resolved PENDING:010) now reports EXPECTED with `VOUCHERNUMBER 1: ALTERID (change-tracking), amounts` — the same run the old banner called a failure; `07-18-22` vs `07-20-42` (the unsaved edit) reports the ambiguous no-movement case and says explicitly that an asserted edit did not land; `06-49-24` vs `06-55-26` (the restart) still reads clean. The FAILURE and NOTEWORTHY branches have no real artifacts and were exercised against synthetic mutations. **Deliberately not built**: content diffing of voucher bodies (ledger names, narration, dates, per-line matching). That needs a real diff engine tolerant of re-ordered ledger entries and is not required to tell a void run from a real one — file it separately if it is ever actually needed. Never promoted to a GitHub issue, so no closing keyword |

## Suggested issue bodies (for when these get filed for real)

**PENDING:001 / PENDING:004 — register extraction**
> `TallyAdapter`/`ZohoAdapter.extract_*` currently raise `NotImplementedError`.
> Implement against the real sandbox once credentials exist (see "Blocked
> on" column). Normalize output to the `LedgerLine` shape in
> `app/books_connector/base.py` — both adapters must produce the same
> schema (TC-03 / ZB-02). Add integration tests per Testing Strategy §2
> (sandbox-only, not in the unit CI gate).

**PENDING:002 / PENDING:005 — posting**
> Implement the write path. CG7's duplicate-check happens in the caller,
> not here — don't re-implement it in the adapter. Money fields must be
> `Decimal` end-to-end (CG5). Add the dual-adapter variant of critical
> flow #1 per Testing Strategy §3 once both PENDING:002 and PENDING:005
> are done.

**PENDING:003 / PENDING:006 — connection health**
> Real health check replacing the current `"unknown"` stub return value.
> Surfaces on the Admin — Connections screen (TC-05 / ZB-05).

**PENDING:007 — TE-02 routing** *(resolved — kept for the record)*
> `escalate_overdue()` currently just sets `status="escalated"` without
> picking a specific assignee. Needs the routing rule table (task_type →
> default role/person) from TE-02, plus the `PUT /admin/routing-rules/{task_type}`
> endpoint already specified in the API Spec §4.
